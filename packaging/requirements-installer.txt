# Build-time dependencies for CraftBotInstaller.exe.
#
# Separate from requirements.txt on purpose: this is the *installer's* own
# dependency, not the agent's. The agent never imports customtkinter, and the
# installer never imports the agent's stack — keeping the two lists apart is
# what stops a 13 MB bootstrapper from quietly growing an ML runtime.
#
# Install alongside pyinstaller before building the spec:
#     pip install -r packaging/requirements-installer.txt
customtkinter==6.0.0
pyinstaller
