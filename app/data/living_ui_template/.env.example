# Living UI Environment Variables
#
# API calls are same-origin (/api is proxied to PocketBase); no URL
# configuration is needed here.

# Add your app's keys and secrets below (VITE_ prefix exposes to frontend)
# VITE_API_KEY=your_api_key_here
