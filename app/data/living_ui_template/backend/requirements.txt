# Living UI Backend Dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pytest>=7.0.0
httpx>=0.24.0
# Multipart parsing for the system file-upload routes (files_routes.py)
python-multipart>=0.0.6
# Postgres driver - lets DATABASE_URL in backend/.env point at
# Supabase/Postgres instead of the default SQLite (see database.py).
psycopg2-binary>=2.9.0
